Sounds are now synthesized procedurally with the Web Audio API in
js/systems/soundSystem.js — no audio files are needed or loaded.

This directory is kept only in case you later want to swap a
generated sound for a real recording; that would require editing
soundSystem.js to play a file for that name instead.
